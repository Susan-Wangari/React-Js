class MyComponent extends React.Component {
  constructor(props) {
    super(props);
  }
  render() {
    // Change code below this line
  return (
    <div>
    <h1>Hello React!</h1>
    </div>
  )


    // super() to call the constructor of the parent class, in this case React.Component
  }
};
//The constructor is a special method used during the initialization of objects that are created with the class keyword
//define a React component with the ES6 class syntax.
